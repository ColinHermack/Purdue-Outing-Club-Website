# Styles Directory
The only thing contained in this directory is a single CSS file with tailwind configuration information and which hides
any x-axis overflow to avoid a horizontal scroll bar being shown. All other styling is done through tailwind.